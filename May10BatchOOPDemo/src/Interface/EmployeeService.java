package Interface;

public interface EmployeeService {
	//we have add one employee with value
	void addEmployee(Employee emp);
	//display employee object with all parameter and value
	Employee displayEmployee(Employee emp);
}
